# src/

Reserved for future tooling and extensions that complement the Copilot
assistant — e.g., custom analyzers, knowledge-ingestion scripts, schema
introspection utilities, or batch-job parsers.

Suggested future layout:

```
src/
├── analyzers/        # static analyzers for Struts / COBOL / T-SQL
├── ingestion/        # scripts that normalize /knowledge entries
├── extractors/       # schema/stored-proc/job extractors
└── tools/            # CLI utilities used by developers
```

No code lives here yet. This file exists to keep the directory tracked.
