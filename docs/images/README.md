# Screenshot Placeholders

Drop hackathon demo screenshots here using the filenames referenced from
[../../README.md](../../README.md). Recommended dimensions: 1600×1000 PNG,
no shadow, light theme for projector visibility.

| Filename | Shows |
|----------|-------|
| `hero.png`           | Title card / repo + Copilot chat side-by-side. |
| `demo-cobol.png`     | Demo 1 — `@legacy-code` explaining `NB_POST.CBL` with the `Z9` citation visible. |
| `demo-review.png`    | Demo 2 — `@review` findings list with severity badges and the do-not-touch check. |
| `demo-schema.png`    | Demo 3 — schema analysis showing inferred FKs and ranked normalization opportunities. |
| `architecture.png`   | Architecture diagram (or screenshot of the rendered mermaid). |
| `routing.png`        | Routing flowchart from [docs/agent-routing.md](../agent-routing.md). |

Until real screenshots exist, the README will show alt text — that's fine
for the repo, and the README itself notes "screenshot placeholder" inline.
